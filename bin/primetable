#!/usr/bin/env ruby

require 'primetable'
require 'timer'

case ARGV[0]
  when "-t"
    Timer.timer{puts PrimeTable.run}
    puts Timer.time
  when "-v"
  	puts "PrimeTable v#{PrimeTable::VERSION}"
  else
    puts PrimeTable.run
end
